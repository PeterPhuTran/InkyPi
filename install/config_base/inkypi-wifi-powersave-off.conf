# Installed by InkyPi to /etc/NetworkManager/conf.d/
# Disables WiFi power saving, a common cause of the Raspberry Pi dropping its
# WiFi connection and not reconnecting until power cycled.
#   2 = disable power save
[connection]
wifi.powersave = 2
